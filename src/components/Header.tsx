import React from 'react';
import logo from '../logo.svg';

const Header = () => {
  return (
    <header className="header" title="ecologi-reporting">
      <img src={logo} className="ecologi-logo" alt="logo" />
    </header>
  );
};

export default Header;
